# Text Generator and Sentiment analysis
# create virtual environment
python -m venv venv

# activate Virtual venv
.\venv\Scripts\activate

# Install required library
pip install -r requirements.txt

# run the server
python app.py
